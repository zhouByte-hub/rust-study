
import { Button } from "std-widgets.slint";
export component Tarbbar {

    Rectangle {
        width: 100%;
        height: 50px;
        background: #68a8ab;
        
        Button {
            width: 80px;
            height: parent.height - 20px;
            text: "+";
        }
    }
    
}