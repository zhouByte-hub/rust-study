[package]
name = "rust_async"
version = "0.1.0"
edition = "2024"

[dependencies]
# futures = "0.3.31"
tokio = {version="1.47.1", features = ["full"]}
tokio-stream = {version = "0.1.17", features = ["sync"] }